// Copy this file to `theme/firebase-config.js` and fill with your Firebase web app config.
// This config is not a secret; it identifies your Firebase project.
// You can find it in Firebase Console → Project settings → Your apps (Web app).

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_APP_ID",
};
